{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm ci --workspaces",
  
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
