{
  "version": 2,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npm run vercel-build",

  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...slug].js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
