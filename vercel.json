{
  "version": 2,
  "buildCommand": "npm --prefix backend ci && npm run build",
  "installCommand": "npm install --include=dev",
  
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
