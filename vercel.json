{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install --workspaces --include=dev",
  
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
